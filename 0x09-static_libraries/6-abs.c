#include "main.h"

/**
 * _abs - computing the absolute value of an integer
 * @n: the int to caheck
 * Return: the absolute value of an integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
