#include "main.h"
/**
 * add - adding 2 integers and returning the result
 * @a: int to be added to b
 * @b: int to be added to a
 * Return: sum of add a and b
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
